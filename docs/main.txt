Need to do on client first:
    2. Place .madmanrc in home (~) folder with with client config values.
    3. Place .madman in project folder with project specific values.




# Git SSH <-> HTTP url convertion
# madman clone x this
# undeploy command

# maintain state in server (needed for undeploy if multiple deployments)
# nginx web page
# check 24x7 server
# configless (fully cmd based)
# multiple deployments
# install on VPS / client script sh with curl
# deployment info to list command
# web-ui via server
